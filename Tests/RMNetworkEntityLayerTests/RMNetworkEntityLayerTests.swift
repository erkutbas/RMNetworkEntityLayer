import XCTest
@testable import RMNetworkEntityLayer

final class RMNetworkEntityLayerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(RMNetworkEntityLayer().text, "Hello, World!")
    }
}
